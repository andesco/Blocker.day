{
  "name": "blocker-day",
  "version": "0.1.0",
  "description": "Blocker.day is a Cloudflare Worker that generates a .ics calendar feed to create deterministic blocks of time",
  "main": "index.js",
  "scripts": {
    "build:readme": "node scripts/build-readme.js",
    "precommit": "npm run build:readme",
    "dev": "npm run build:readme && wrangler dev",
    "deploy": "wrangler deploy"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.7.2"
  },
  "dependencies": {
    "marked": "^15.0.11"
  }
}
